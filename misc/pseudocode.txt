1) Download new images

2) go over each coordinate in Array to see if pixel is blue
	if blue:
		- add to counter
	else:
		- move to next pixel
		
3) divide counter# / total number in array

	if % is > 50%:
		- send email
		
